export type RevealControls = {
  title: string;
  subtitle: string;
  iconUrl: string;
  iconCornerRadius: number;
  badgePrefix: string;
  badgeLabel: string;
  badgeIconUrl: string;
  durationMs: number;
  playbackRate: number;
  glowColor: string;
  rimColor: string;
  grayColor: string;
};
